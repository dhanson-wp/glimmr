# Seeding scratch (demo content import staging — never part of the theme)
/_seed/

# Local design notes — kept on disk, not part of the distributed theme
/docs/

# Demo-only photos — for local review, not distributed. Bring your own.
/assets/img/
/screenshot.png

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Node (only if a build is ever added)
node_modules/
